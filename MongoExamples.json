translate these to Mongo query:


if (widget.color === 'blue' && widget.size > 10)

{
	$and: [
		{
			"color": "blue"
		},
		{
			"size": {
		$gt: 10
			}
		}
	]
}


if (widget.color === 'green' && widget.size <= 5)

{
	$and: [
		{
			"color": "blue"
		},
		{
			"size": {
				$lte: 5
			}
		}
	]
}

if (widget.color === 'green'  && widget.size > 10 || (widget.color === 'green' && widget.size <= 5))

{
	$and: [
		{
			"color": "blue"
		},
		{
			"size": {
		$gt: 10
			}
		}
	]
	$or: [
		{
			"color": "blue"
		},
		{
			"size": {
				$lte: 5
			}
		}
	]
}

// the above is wrong.  This is saying either the first two AND OR the second two AND so the OR operator comes first, then the AND operators are nested inside that